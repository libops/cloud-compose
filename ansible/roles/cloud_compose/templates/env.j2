{# cloud-compose-env-contract: Docker Compose double-quoted dotenv data; never source this file #}
{% for item in _cc_env | dict2items | sort(attribute='key') %}
{{ item.key }}="{{ item.value | string | replace("\\", "\\\\") | replace('"', '\\"') | replace("$", "$$") | replace("\n", "\\n") | replace("\r", "\\r") | replace("\t", "\\t") }}"
{% endfor %}
